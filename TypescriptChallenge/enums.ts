enum options {
  DELETE = "/delete",
  CREATE = "/create",
  UPDATE = "/put",
}

enum numeric {
  first,
  second,
  third,
  fourth,
}

console.log(numeric);

enum strings {
  Up = "UP",
  Down = "DOWN",
}

console.log(strings);
